{/* imported from repo/docs/04-Config-and-Env-Matrix.md; review for secrets/PII before publishing */}

| Key | Default | Source | Read At (file:line) | Behavior if Missing | Security Notes |
| --- | --- | --- | --- | --- | --- |
| `Server.Port` | `8089` | `config.yaml`^config.yaml:12^ | Listener binds via `net.Listen("tcp", :Port)`^main.go:2114^ | `main.go` fatals if bind fails, preventing start.^main.go:2116^ | Collisions expose gateway; keep port behind LB/security groups. |
| HTTP server timeouts (`WriteTimeout`, `ReadTimeout`, `IdleTimeout`) | `60s`, `60s`, `120s` (constants) | Hard-coded in `http.Server` construction^main.go:2128^main.go:2132^ | Values baked into runtime; no config fallback so defaults always apply. | Adjustments require code change; tune to upstream SLAs and keep-alive needs. | Expose via config to enforce environment-specific backpressure policies. |
| `Security.JWTSecretKey` | Base64 string in YAML | `config.yaml`^config.yaml:39^ | Validated in config loader; fatal if empty.^config/config.go:629^ | `Validate()` returns error, panic during startup.^config/config.go:642^ | Check in env instead of repo; rotate regularly. |
| `Security.bootstrap_token` | Same secret as JWT | `config.yaml`^config.yaml:44^ | Required header in `/bootstrap` controller.^controller/bootstrap_controller.go:107^ | Missing/incorrect token returns `403` to caller.^controller/bootstrap_controller.go:110^ | Treat as one-time secret; move to secret store to avoid git exposure. |
| `flags.onboarding.enabled` | `false` | `config.yaml`^config.yaml:48^ | Checked before onboarding services/controllers are initialised.^config/config.go:120^main.go:141^ | When `false` routes are not registered and the wizard is unavailable.^main.go:2003^controller/onboarding_controller.go:45^ | Keep disabled until onboarding is ready per environment; flag is OFF by default. |
| `OAuth2.JWKSURL` | Unset | YAML/env (`OAuth2` block) | Middleware config logs warning when empty.^main.go:1848^main.go:1863^ | Missing JWKS causes validation failure path; requests allowed only if other authenticators pass.^main.go:1864^ | Populate from IdP; avoid relying on warning-level detection. |
| `RateLimit.redis_enabled`, `redis_address`, `RequestsPerMinute`, `BurstSize` | Enabled, `redis:6379`, `1000`, `100` | `config.yaml`^config.yaml:24^config.yaml:27^ | New rate-limit middleware uses config map & Redis client.^main.go:1913^middleware/ratelimit.go:186^ | If Redis unavailable, falls back to in-memory limiter and logs warning.^middleware/ratelimit.go:193^middleware/ratelimit.go:204^ | Redis credentials optionally pulled from env; ensure TLS/ACL where deployed.^config/config.go:747^ |
| `Redis.Host/Port/Password/DB` | Host `redis`, port `6379`, password empty, DB `0` | `REDIS_*` env with defaults^config/config.go:747^config/config.go:759^ | `FillFromEnv` populates before client initialization.^main.go:1238^main.go:1243^ | Missing host defaults to `redis`; connection failure fatals during startup.^main.go:1255^main.go:1259^ | Provide secrets via env; enable auth in production. |
| `Caching.Enabled`, `Caching.APIDefinitionsTTLSeconds` | `true`, `300` | `config.yaml`^config.yaml:192^ | Governs admin-service cache TTL and in-process caching.^main.go:1400^main.go:1408^ | Disabled caching logs info; TTL fallback to 5 minutes.^main.go:1406^main.go:1411^ | In-memory cache has no eviction; monitor memory/correct TTLs.^main.go:3158^main.go:3216^ |
| `Tracing.JaegerAgentHost/Port` | Defaults `localhost:6831` when env absent | `JAEGER_AGENT_HOST`, `JAEGER_AGENT_PORT` env^main.go:212^main.go:220^ | `initTracing` sets warning defaults and continues without tracing on error.^main.go:212^main.go:224^ | If exporter creation fails, tracing disabled but gateway still boots.^main.go:207^main.go:227^ | Set agent host/port per environment; secure Jaeger collector network. |
| `Consul.Address` | `"consul-server:8500"` | `config.yaml`^config.yaml:66^ | Overrides apply from config or `CONSUL_HTTP_ADDR`; logs selected address.^main.go:1285^main.go:1295^ | Failure to contact agent fatals startup to avoid stale routing.^main.go:1310^main.go:1316^ | Ensure Consul credentials/ACL integrated; sanitize logs where necessary. |
| `Apis[*].rate_limit` (enabled, burst, strategy) | API-specific block | `config.yaml`^config.yaml:219^ | Merged into rate-limit map per API for middleware.^main.go:1913^middleware/ratelimit.go:274^ | Invalid period strings log warnings; limiter falls back to global.^middleware/ratelimit.go:211^middleware/ratelimit.go:215^ | Coordinate with tenant traffic budgets; per-org keys require auth context.^middleware/ratelimit.go:297^ |
| `CORS Allowed Origins` | `{ "http://localhost:3000" }` | Hard-coded map in middleware | `corsMiddleware` checks allowed map; not config-driven.^main.go:2680^main.go:2689^ | Requests from other origins lose CORS headers; preflight fails.^main.go:2695^main.go:2699^ | Externalize and validate to avoid overly permissive wildcards. |
